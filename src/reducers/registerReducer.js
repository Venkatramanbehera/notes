let initialRegisterState = false

const registerReducer = (state = initialRegisterState, action) => {
    switch(action.type){

        default :
            return false
    }
}

export default registerReducer